張園 full-history WhatsApp import + bulk GPT review

Replace only:
- reader/server.js
- web/app/admin/page.js

Do NOT change:
- Dockerfile
- whatsapp-web.js patches
- Railway volume
- READER_GPT_KEY
- your working OpenAPI v1.0.2 Action schema

After deploy:
1. Open the whatsapp-reader homepage.
2. Click "Import ALL prospect leads + full message history".
3. The import runs in the background. Watch its JSON status.
4. Wait until running=false and completedAt has a value.
5. Open /admin. It now shows total messages stored and message count per lead.
6. Add CUSTOM_GPT_BULK_INSTRUCTIONS_v1.0.3.md to the existing Custom GPT instructions.
7. Ask the GPT:
   "Analyse every unreviewed renter lead and save the results. Continue until there are zero unreviewed prospects."

The full import:
- scans every direct WhatsApp chat available to the connected WhatsApp Web session;
- matches "To organise viewing" or "Viewings -";
- excludes chats also labelled "Tenants";
- loads the complete available history using fetchMessages({limit: Infinity});
- is safe to rerun because existing WhatsApp message IDs are not duplicated.
