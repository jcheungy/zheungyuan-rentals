Zheungyuan Rentals – Detached Frame Fix

Replace:
reader/server.js

with this version and commit to main.

Changes:
- /labels now uses client.getLabels() directly instead of scanning every chat.
- Each label uses label.getChats() for its chat count.
- Adds retry handling for transient Puppeteer errors:
  Attempted to use detached Frame
  Execution context was destroyed
- /sync/chats with a label now resolves that label first and imports only chats attached to it.

After Railway redeploys:
1. Open /qr and confirm WhatsApp is connected.
2. Open /labels.
3. Send the JSON result back.
4. Do NOT run /sync/chats yet until we confirm the rental label.
