Replace GitHub file:
reader/server.js

with this server.js and commit to main.

This version:
- keeps the existing WhatsApp session/QR setup
- keeps the Railway Chromium lock cleanup
- keeps detached-frame retry handling
- avoids label.getChats()/getChatsByLabelId(), which was producing the cryptic "r" error
- scans recent direct chats and checks each chat's labels
- imports only chats labelled "Tenants"
- adds a "Sync Tenants" button to the reader homepage
- returns much better error detail if a chat fails

After Railway redeploy:
1. Open the reader homepage.
2. Confirm Status = Connected.
3. Click Sync Tenants.
4. Send the JSON shown below the button.
