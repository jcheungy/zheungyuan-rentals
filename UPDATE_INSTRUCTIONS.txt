Zheungyuan Rentals – Browser Sync Update

Replace:
reader/server.js

with this version and commit to main.

This keeps:
- WhatsApp QR / LocalAuth
- Railway Chromium lock cleanup
- detached-frame retry
- /labels
- label-filtered /sync/chats

It adds a simple browser button on the reader home page:
"Sync Tenants"

Defaults:
- 20 chats
- 150 messages per chat
- label = Tenants

After Railway redeploys:
1. Open the whatsapp-reader home URL.
2. Confirm Status = Connected.
3. Click "Sync Tenants".
4. Copy the JSON result shown on the page and send it to ChatGPT.
5. Then open the Zheungyuan /admin dashboard.

No curl or terminal command is required.
