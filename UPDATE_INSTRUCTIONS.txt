Zheungyuan Rentals – Chromium Lock Fix

Replace:
reader/server.js

with this server.js and commit to main.

What changed:
- Keeps your existing WhatsApp LocalAuth session.
- Before Chromium starts, removes only stale Railway/Chromium lock files:
  SingletonLock
  SingletonSocket
  SingletonCookie
- Does NOT delete the WhatsApp authentication profile or Railway volume.
- Retains the /labels endpoint and label-filtered /sync/chats endpoint from the previous update.

After Railway redeploys:
1. Open the whatsapp-reader service logs.
2. Look for either:
   "Removed stale Chromium lock: ..."
   and then
   "WhatsApp ready"
3. Open /qr. It should say "WhatsApp is connected."
4. Then open /labels and send the JSON result back to ChatGPT.

Do not delete the Railway volume.
